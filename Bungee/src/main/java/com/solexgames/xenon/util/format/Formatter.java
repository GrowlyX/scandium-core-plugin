package com.solexgames.xenon.util.format;

import co.aikar.commands.CommandHelpFormatter;
import co.aikar.commands.CommandManager;

public class Formatter extends CommandHelpFormatter {

    public Formatter(CommandManager manager) {
        super(manager);
    }
}
